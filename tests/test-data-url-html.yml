name: Test data-url HTML generation (no input file needed)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Download vizb binary
        run: |
          mkdir -p /tmp/vizb-install
          TAG="v0.15.0"
          VERSION="0.15.0"
          URL="https://github.com/goptics/vizb/releases/download/${TAG}/vizb@${VERSION}-linux-amd64.tar.gz"
          curl -sfL "$URL" -o /tmp/vizb-archive.tar.gz
          tar xzf /tmp/vizb-archive.tar.gz -C /tmp/vizb-install
          chmod +x /tmp/vizb-install/vizb

      - id: action
        uses: .
        with:
          data-url: "https://example.com/data.json"
          output-html: "/tmp/data-url-output.html"
          vizb-binary: /tmp/vizb-install/vizb

      - name: Assert HTML output file exists
        run: |
          if [ ! -f "/tmp/data-url-output.html" ]; then
            echo "FAIL: expected HTML output file /tmp/data-url-output.html not found"
            exit 1
          fi
          echo "PASS: HTML output file exists"

      - name: Assert HTML file contains expected content
        run: |
          if grep -qi "<html" /tmp/data-url-output.html || grep -qi "<!DOCTYPE" /tmp/data-url-output.html; then
            echo "PASS: HTML file contains HTML content"
          else
            echo "FAIL: HTML file does not appear to contain HTML content"
            head -5 /tmp/data-url-output.html || true
            exit 1
          fi

      - name: Assert output-file-html output
        env:
          ACTUAL: ${{ steps.action.outputs.output-file-html }}
        run: |
          echo "actual: $ACTUAL"
          if [ "$ACTUAL" != "/tmp/data-url-output.html" ]; then
            echo "FAIL: expected output-file-html to be '/tmp/data-url-output.html', got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"
