name: Test basic CSV cmd input to JSON output
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Download vizb binary
        run: |
          mkdir -p /tmp/vizb-install
          TAG="v0.15.0"
          VERSION="0.15.0"
          URL="https://github.com/goptics/vizb/releases/download/${TAG}/vizb@${VERSION}-linux-amd64.tar.gz"
          curl -sfL "$URL" -o /tmp/vizb-archive.tar.gz
          tar xzf /tmp/vizb-archive.tar.gz -C /tmp/vizb-install
          chmod +x /tmp/vizb-install/vizb

      - id: action
        uses: .
        with:
          cmd: "printf 'region,latency,sales\\nAsia,12,100\\nEurope,8,80\\nNorth America,15,120\\n'"
          name: "Region Metrics"
          output-json: "/tmp/output.json"
          parser: csv
          vizb-binary: /tmp/vizb-install/vizb

      - name: Assert JSON output file exists
        run: |
          if [ ! -f "/tmp/output.json" ]; then
            echo "FAIL: expected JSON output file /tmp/output.json not found"
            exit 1
          fi
          echo "PASS: JSON output file exists"

      - name: Assert JSON output is valid JSON
        run: |
          if ! python3 -c "import json,sys; json.load(open('/tmp/output.json'))" 2>/dev/null; then
            echo "FAIL: output.json is not valid JSON"
            cat /tmp/output.json || true
            exit 1
          fi
          echo "PASS: output.json is valid JSON"

      - name: Assert output-file-json output matches
        env:
          ACTUAL: ${{ steps.action.outputs.output-file-json }}
        run: |
          echo "actual: $ACTUAL"
          if [ "$ACTUAL" != "/tmp/output.json" ]; then
            echo "FAIL: expected output-file-json to be '/tmp/output.json', got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"
